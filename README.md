# Колесо фортуны

Точная копия онлайн-рулетки с [piliapp.com](https://ru.piliapp.com/random/wheel/) — инструмент для случайного выбора с красивым анимированным колесом.

## Возможности

- 🎡 **Красивое анимированное колесо** — плавное вращение с реалистичной физикой
- ⌨️ **Горячие клавиши** — полное управление без мыши
- 📱 **Адаптивный дизайн** — работает на мобильных и десктопе
- 💾 **Сохранение состояния** — ваши элементы и настройки сохраняются в браузере
- 🔊 **Звуковые эффекты** — оповещения при вращении и выборе
- ⚙️ **Настройки** — авто-скрытие выбранных элементов, вкл/выкл звук
- 🎨 **Множество цветов** — до 200 элементов с автоматической раскраской

## Технологии

- **React 18** — UI библиотека
- **Vite** — быстрый сборщик
- **TypeScript** — типобезопасность
- **Tailwind CSS** — стилизация
- **Canvas API** — отрисовка колеса
- **Web Audio API** — звуковые эффекты

## Установка и запуск

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Собрать для продакшена
npm run build

# Предпросмотр продакшен-сборки
npm run preview
```

После запуска `npm run dev` откройте http://localhost:5173 в браузере.

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `SPACE` | Крутить колесо |
| `X` | Закрыть рекламный блок |
| `S` | Скрыть выбранный элемент |
| `R` | Сбросить колесо |
| `E` | Открыть редактор списка |
| `F` | Полноэкранный режим |

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Modals/         # Модальные окна
│   ├── WheelCanvas.tsx # Отрисовка колеса
│   ├── SidebarList.tsx # Список элементов
│   ├── Controls.tsx    # Кнопки управления
│   ├── HotkeysHelp.tsx # Подсказки горячих клавиш
│   ├── Footer.tsx      # Футер
│   └── AdPlaceholder.tsx # Плейсхолдер рекламы
├── hooks/              # Custom React hooks
│   ├── useLocalStorage.ts    # Сохранение в LS
│   ├── useWheelAnimation.ts  # Анимация вращения
│   └── useHotkeys.ts         # Обработка горячих клавиш
├── utils/              # Утилиты
│   ├── wheelColors.ts  # Палитра цветов
│   ├── wheelPhysics.ts # Физика вращения
│   └── sound.ts        # Звуковые эффекты
├── types/              # TypeScript типы
└── App.tsx             # Главный компонент
```

## Как пользоваться

1. **Добавление элементов** — введите элементы в textarea справа (по одному в строке)
2. **Вращение** — нажмите кнопку "Старт", кликните на колесо или нажмите SPACE
3. **Результат** — выбранный элемент будет подсвечен в списке
4. **Сброс** — нажмите "Сбросить" или R для возврата в исходное состояние
5. **Редактирование** — используйте кнопки "Заголовок", "Изменить", "скрывать…" для настроек

## Особенности

- Максимум 200 элементов
- Пустые строки игнорируются
- Колесо автоматически перерисовывается при изменении списка
- Выбранный элемент можно автоматически скрыть (в настройках)
- Поддержка Retina/HiDPI экранов
- Работает в Chrome, Firefox, Safari

## Лицензия

MIT

## Ссылка на оригинал

[ru.piliapp.com/random/wheel/](https://ru.piliapp.com/random/wheel/)
