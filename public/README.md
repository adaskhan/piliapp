# 🎡 Колесо Фортуны — Vanilla JS

**Чистый JavaScript проект** без фреймворков. Многостраничный сайт с колесами фортуны для случайного выбора.

## 🚀 Быстрый старт

**Никакого npm, ни сборки!** Просто откройте HTML файлы в браузере:

```bash
# Открой главную страницу
open public/index.html

# Или запусти простой HTTP сервер
cd public
python3 -m http.server 8000
# Затем открой http://localhost:8000
```

## 📁 Структура проекта

```
public/
├── css/
│   ├── piliapp.css       # Основные стили (navbar, layout, sidebar)
│   └── wheel.css         # Стили колеса (exact copy from piliapp)
├── js/
│   ├── presets.js        # Все категории (vegetables, animals, cities...)
│   ├── storage.js        # LocalStorage модуль
│   ├── wheel-core.js     # Ядро колеса (canvas + animation)
│   └── page-wheel.js     # Логика страницы
├── index.html           # Главная страница (колесо по умолчанию)
├── vegetables.html      # Овощи
├── animals.html         # Животные
├── cities.html          # Города
├── yes-no.html          # Да/Нет
├── emojis.html          # Смайлики
├── meals.html           # Выбор еды
├── fruits.html          # Фрукты
├── would-you-rather.html # Что бы ты предпочел?
├── never-have-i-ever.html # Я никогда не...
├── two-truths-and-a-lie.html # Две правды и ложь
├── charades-pictionary.html # Шарады
├── name-that-tune.html  # Угадай мелодию
└── scavenger-hunt.html  # Охота за сокровищами
```

## ✨ Возможности

- **🎯 Canvas отрисовка колеса** — четкое изображение на retina дисплеях
- **🔄 CSS spin animation** — плавное вращение через cubic-bezier
- **💾 LocalStorage** — сохранение пользовательских списков для каждой страницы
- **⌨️ Горячие клавиши**:
  - `SPACE` — крутить
  - `R` — сбросить
  - `E` — редактировать
  - `F` — во весь экран
  - `S` — скрыть выбранный элемент
- **📱 Адаптивный дизайн** — 320px / 360px / 540px через media queries
- **🎨 1:1 копия piliapp** — точное воспроизведение DOM и CSS

## 🏗️ Архитектура

### Модули

#### `presets.js`
```javascript
const PRESETS = {
  vegetables: {
    title: "Какой овощ вы собираетесь съесть?",
    names: ["🍅 Помидор", "🥕 Морковь", ...],
    url: "vegetables.html"
  },
  // ... другие категории
};
```

#### `storage.js`
```javascript
Storage.saveItems(pageKey, items);
Storage.loadItems(pageKey);
Storage.saveTitle(pageKey, title);
Storage.loadSettings();
```

#### `wheel-core.js`
```javascript
WheelCore.init(canvas, names, options);
WheelCore.spin();
WheelCore.reset();
WheelCore.setNames(newNames);
```

#### `page-wheel.js`
Главная логика страницы:
- Загружает preset для текущей страницы
- Инициализирует колесо
- Обрабатывает hotkeys
- Управляет редактированием списка

### CSS

**piliapp.css** — общие стили:
- Navbar
- 3-колоночный layout
- Sidebar с textarea
- Footer категории
- Hotkeys панель

**wheel.css** — стили колеса:
- Адаптивные размеры
- Wheel-bg с shadow
- Wheel-pin (красный указатель)
- Wheel-center (белый центр)
- Overlay "нажмите, чтобы крутить"

## 🎨 Добавление новой категории

1. Добавь в `presets.js`:
```javascript
your-category: {
  title: "Ваш заголовок",
  names: ["Элемент1", "Элемент2", ...],
  url: "your-category.html"
}
```

2. Создай `your-category.html` (копируй любой существующий файл)

3. Обнови навигацию во всех файлах (добавь активный класс)

## 📝 DOM структура (exact copy from piliapp)

```html
<div id="results">
  <div id="wheel-wrapper">
    <div id="pls-click">нажмите, чтобы крутить</div>
    <span id="test-text"></span>
    <div id="wheel-bg"></div>
    <canvas id="wheel" width="540" height="540"></canvas>
    <span id="wheel-center"></span>
    <span id="wheel-pin">▼</span>
  </div>
</div>
```

## 🔧 Настройки

LocalStorage ключи:
- `{pageKey}:names` — список элементов
- `{pageKey}:title` — заголовок страницы
- `wheel:settings` — глобальные настройки

## 📦 Без зависимостей

✅ Чистый HTML/CSS/JS
❌ Нет React
❌ Нет Vue
❌ Нет Angular
❌ Нет Tailwind
❌ Нет npm/webpack/vite
❌ Нет build процесса

## 🌐 Совместимость

Работает во всех современных браузерах:
- Chrome/Edge (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Mobile браузеры

## 📄 Лицензия

MIT

---

Создано с ❤️ используя чистый JavaScript
